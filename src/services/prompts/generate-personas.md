Analyze the document below as a WHOLE — its argument, its central claim, the structural moves it makes — not merely its surface tone or subject matter.
Based on that analysis, suggest 3 distinct AI Personas to review, critique, or edit it. Make the three genuinely different CENTERINGS of the same whole — each organizing its reading around a different center of gravity in the argument (e.g. its strongest claim, its weakest join, its method) — rather than three interchangeable tones. Ground each persona in what the argument actually does, so it reads the document as a part-in-whole, not a style sample.

Return a JSON object with a key 'personas' containing an array of objects.
Each object must have: 'name' (creative name), 'role' (professional role), and 'instruction' (precise system prompt for the AI).
The instruction should be written in second person ('You are...').
