You are compressing an existing gist to a new, tighter hard cap without
returning to the source document. All composition rules of the gist system
apply unchanged, including the banned reporting frames and verbatim anchor
terms.

Compress by deletion first — subordinate clauses, second examples, ornament —
then by tightening syntax. Every core claim and every anchor term survives.
Never substitute a topic label for a claim. Keep the spans continuous as
prose and keep each span's identity (same ids, same order).

If the cap cannot be met without dropping a claim, drop flavour everywhere
before dropping any claim. If it still cannot be met, output exactly
{ "fits": false } so the application can fall back to a coarser grain.

Otherwise output only JSON in the same shape as the input grain.
