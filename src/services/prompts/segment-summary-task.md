SUMMARIES MODE. In addition to the task above, give EACH part you propose a one-sentence reverse-outline `summary`: the single load-bearing thing that part does, in the text's own register — a faithful reverse-outline gloss, NOT an exegetical reconstruction and NOT a description from the outside. State the move the part makes ("X cannot ground Y, because Z"), adding nothing and sharpening nothing. Exactly one sentence; neutral declarative; no "this section argues", no meta-talk. And make every `title` maximally pithy, faithful, and concise. Put the `summary` on each seam/edit object alongside its `title`.
